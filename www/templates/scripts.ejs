<script type="module">
   import { init } from '/js/main.js';

   // async module scripts are deferred by spec — the DOM is already parsed when
   // this runs, so DOMContentLoaded would have already fired. Just init
   // directly.
   init().catch(error => {
      console.error("Failed to initialize application:", error);
   });
</script>